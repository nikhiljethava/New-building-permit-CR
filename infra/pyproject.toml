[project]
name = "infra"
version = "0.1.0"
description = "Deploys the infrastructure for building-permit app"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-cloud-aiplatform>=1.143.0",
]
